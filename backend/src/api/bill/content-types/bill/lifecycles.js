module.exports = {
  async afterCreate(event) {
    let param = event.params;

    // let knex = await strapi.db.connection.context.raw(
    //   "select sum(total) from bills"
    // );
    //    ram.result = { "result" : param.result, "extra" }
  },
};
